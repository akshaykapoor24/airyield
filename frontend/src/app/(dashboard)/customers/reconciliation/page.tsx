import ComingSoon from "@/components/ui/ComingSoon";

// Reconciliation → Customer Reconciliation (it used to sit under Customer data). The
// route is unchanged so existing links keep working.
export default function CustomerReconciliationPage() {
  return (
    <ComingSoon
      title="Customer Reconciliation"
      description="Customer reconciliation will be managed here."
    />
  );
}
