// Statements reuse the generic, server-column-driven repository component built
// for Airline Adjustments. It takes a full `apiBase` prop, so any spec-driven
// statement type renders through it with zero per-type UI.
export { default } from "@/components/adjustments/AdjustmentRepository";
